{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Docker: Attach to Node",
            "type": "node",
            "request": "attach",
            "protocol": "inspector",
            "port": 5858,
            "address": "localhost",
            "localRoot": "${workspaceRoot}",
            "remoteRoot": "/app/"
        }
    ]
}